# 变更：添加LLM决策框架

## 概述
本变更提案旨在为系统添加一个LLM（大语言模型）决策框架，该框架能够接收用户输入，调用大模型来决定需要执行的服务方法。这将使系统能够智能化地根据用户意图自动选择和执行适当的服务。

## 背景
当前系统已实现了AI服务注解系统和驱动框架，允许服务被注册和调用。但缺少一个智能决策层，可以根据用户的自然语言输入来决定调用哪个服务。为了实现真正的AI驱动自动化，需要一个能够解析用户意图并映射到适当服务的决策层。

## 影响
- 创建LLM决策服务，用于解析用户意图
- 实现意图到服务的映射机制
- 集成现有AI服务框架
- 提供智能服务调用接口