# 变更：添加AI服务注解系统

## 概述
本变更提案旨在为系统添加AI服务注解系统，通过注解自动生成JSON Schema，使大模型能够更好地理解和使用系统中的原子业务或工具。

## 背景
当前系统缺乏一种标准化的方式让大模型理解可用的服务及其参数。为了实现大模型驱动的框架，需要一种机制来描述服务接口，以便大模型能够正确地调用这些服务。

## 影响
- 添加AI服务注解（AIService、AIParam）
- 实现JSON Schema生成器
- 创建运行时服务发现机制
- 更新现有服务以使用新注解